//   Level 1: Rocky plateau
// © https://github.com/Dvrkstvr
var loadouts = import User/loadouts


?loc = rocky
  ?harvest
    equip shovel
  ?pickup.distance < 6
    loadouts.healstvr()
  ?foe = dysangelos_bearer
    loadouts.MeleeShield("","")
  ?foe = dysangelos_elementalist
    ?foe = poison
      loadouts.MeleeShield(ice,ice)
    :?foe = vigor
      loadouts.MeleeShield(poison,poison)
    :?foe = aether
      loadouts.MeleeShield(vigor,vigor)
    :?foe = fire
      loadouts.MeleeShield(aether,aether)
    :?foe = ice
      loadouts.MeleeShield(fire,fire)
  ?foe = dysangelos_perfected
    loadouts.MeleeShield("","")
    ?foe.armor > 0
      loadouts.MeleeHammer("","")
    ?(hp + armor) <= 30
      activate potion
